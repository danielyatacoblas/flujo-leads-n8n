{
  "name": "Club STEM · Leads (producción)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "lead",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "wh-1",
      "name": "Webhook · Nuevo lead",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -260,
        300
      ],
      "webhookId": "club-stem-lead"
    },
    {
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "REEMPLAZAR_ID_HOJA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Leads",
          "mode": "name"
        },
        "options": {}
      },
      "id": "gs-read",
      "name": "CRM · Leer hoja",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        -40,
        300
      ],
      "alwaysOutputData": true,
      "notes": "Hoja CRM: columnas nombre,email,telefono,mensaje,canal,fecha,segmento,estado"
    },
    {
      "parameters": {
        "jsCode": "// Nodo Code de n8n — \"Procesar lead\"\n// Espejo en JavaScript de src/flujo_leads.py (misma lógica, misma salida).\n// La paridad entre ambos está cubierta por tests/test_paridad_js.py.\n// Se inyecta en el workflow con: python scripts/build_workflow.py\n//\n// El CRM demo vive en workflowStaticData para que el flujo funcione SIN\n// credenciales de Google Sheets (en producción lo reemplaza el nodo Sheets).\n\nconst KEYWORDS = {\n  donacion: ['donar', 'donacion', 'aporte', 'auspicio', 'sponsor', 'empresa', 'rse'],\n  voluntariado: ['voluntari', 'ayudar', 'ensenar', 'mentor', 'apoyar'],\n  talleres: ['taller', 'clase', 'curso', 'robot', 'program', 'stem', 'inscri', 'hij', 'alumn'],\n};\n\nconst BIENVENIDA = {\n  talleres: '¡Hola {nombre}! Gracias por tu interés en los talleres STEM del Club. Aquí tienes el calendario del mes y el enlace de inscripción.',\n  voluntariado: '¡Hola {nombre}! Nos alegra que quieras ser voluntario/a. Te contamos cómo funciona el programa y los próximos onboarding.',\n  donacion: 'Hola {nombre}, gracias por querer apoyar al Club STEM. Te compartimos las formas de aportar y el impacto de cada una.',\n  general: '¡Hola {nombre}! Gracias por escribirnos al Club STEM. Un miembro del equipo te contactará muy pronto.',\n};\n\n// Quita tildes/diéresis para que la segmentación no dependa de cómo escriban.\n// Se usa el rango de marcas diacríticas combinantes en escapes Unicode\n// (evita caracteres invisibles en el código fuente).\nconst sinTildes = (s) => s.normalize('NFD').replace(/[̀-ͯ]/g, '');\n\nconst titleCase = (s) =>\n  s.split(/\\s+/).filter(Boolean)\n    .map((w) => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase())\n    .join(' ');\n\n// ── Nodo 1: normalizar ──\nfunction normalizarLead(crudo) {\n  const nombre = titleCase(String(crudo.nombre || '').trim());\n  const email = String(crudo.email || '').trim().toLowerCase();\n  let telefono = String(crudo.telefono || '').replace(/[^\\d+]/g, '');\n  if (telefono.startsWith('+51')) {\n    // ya está en formato E.164\n  } else if (telefono.startsWith('51') && telefono.length === 11) {\n    telefono = '+' + telefono;\n  } else if (telefono.length === 9) {\n    telefono = '+51' + telefono;\n  }\n  return {\n    nombre,\n    email,\n    telefono,\n    mensaje: String(crudo.mensaje || '').replace(/\\s+/g, ' ').trim(),\n    canal: String(crudo.canal || 'web').trim().toLowerCase() || 'web',\n    fecha: crudo.fecha || new Date().toISOString().slice(0, 19),\n  };\n}\n\n// ── Nodo 2: validar ──\nfunction validarLead(lead) {\n  if (!lead.nombre) return { ok: false, motivo: 'sin nombre' };\n  const emailOk = /^[^@\\s]+@[^@\\s]+\\.[^@\\s]{2,}$/.test(lead.email);\n  const telOk = /^\\+51\\d{9}$/.test(lead.telefono);\n  if (!emailOk && !telOk) {\n    return { ok: false, motivo: 'sin email ni teléfono válidos' };\n  }\n  return { ok: true, motivo: 'ok' };\n}\n\n// ── Nodo 3: deduplicar ──\nfunction esDuplicado(lead, crm) {\n  return crm.some((f) =>\n    (lead.email && lead.email === f.email) ||\n    (lead.telefono && lead.telefono === f.telefono));\n}\n\n// ── Nodo 4: segmentar ──\nfunction segmentar(lead) {\n  const texto = sinTildes((lead.mensaje || '').toLowerCase());\n  for (const seg of ['donacion', 'voluntariado', 'talleres']) {\n    if (KEYWORDS[seg].some((k) => texto.includes(k))) return seg;\n  }\n  return 'general';\n}\n\n// ── Ejecución ──\n// En producción el CRM llega del nodo Google Sheets anterior\nconst store = { crm: $('CRM · Leer hoja').all().map((i) => i.json) };\n\nconst salida = [];\n\nfor (const item of $input.all()) {\n  const crudo = item.json.body || item.json;   // el webhook entrega el payload en .body\n  const lead = normalizarLead(crudo);\n\n  const v = validarLead(lead);\n  if (!v.ok) {\n    salida.push({ json: { resultado: 'invalido', motivo: v.motivo, lead } });\n    continue;\n  }\n\n  if (esDuplicado(lead, store.crm)) {\n    salida.push({ json: { resultado: 'duplicado', lead } });\n    continue;\n  }\n\n  const segmento = segmentar(lead);\n  const filaCrm = Object.assign({}, lead, { segmento, estado: 'nuevo' });\n  store.crm.push(filaCrm);\n\n  salida.push({\n    json: {\n      resultado: 'nuevo',\n      segmento,\n      lead,\n      crm_fila: filaCrm,\n      crm_total: store.crm.length,\n      newsletter: {\n        email: lead.email,\n        tag: segmento,\n        accion: lead.email ? 'suscribir' : 'omitir',\n      },\n      email_bienvenida: BIENVENIDA[segmento].replace('{nombre}', lead.nombre.split(' ')[0]),\n      notificacion_equipo:\n        '🔔 Nuevo lead [' + segmento + '] ' + lead.nombre +\n        ' (' + lead.canal + ') — ' + (lead.email || lead.telefono),\n    },\n  });\n}\n\nreturn salida;\n"
      },
      "id": "code-1",
      "name": "Procesar lead",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        180,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "cif",
              "leftValue": "={{ $json.resultado }}",
              "rightValue": "nuevo",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "if-1",
      "name": "¿Es lead nuevo?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        400,
        300
      ]
    },
    {
      "parameters": {
        "operation": "append",
        "documentId": {
          "__rl": true,
          "value": "REEMPLAZAR_ID_HOJA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Leads",
          "mode": "name"
        },
        "columns": {
          "mappingMode": "autoMapInputData",
          "value": {}
        },
        "options": {}
      },
      "id": "gs-append",
      "name": "CRM · Registrar lead",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        640,
        180
      ]
    },
    {
      "parameters": {
        "list": "REEMPLAZAR_ID_LISTA",
        "email": "={{ $json.crm_fila.email }}",
        "status": "subscribed",
        "options": {
          "tagsUi": {
            "tagValues": [
              {
                "name": "={{ $json.segmento }}"
              }
            ]
          }
        }
      },
      "id": "mc-1",
      "name": "Newsletter · Suscribir",
      "type": "n8n-nodes-base.mailchimp",
      "typeVersion": 1,
      "position": [
        880,
        100
      ],
      "notes": "Alternativa gratuita: HTTP Request a la API de Brevo"
    },
    {
      "parameters": {
        "chatId": "REEMPLAZAR_CHAT_ID",
        "text": "={{ $json.notificacion_equipo }}",
        "additionalFields": {}
      },
      "id": "tg-1",
      "name": "Equipo · Avisar por Telegram",
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [
        880,
        260
      ]
    },
    {
      "parameters": {},
      "id": "noop-dup",
      "name": "Duplicado · Solo log",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        640,
        420
      ]
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "hours",
              "hoursInterval": 6
            }
          ]
        }
      },
      "id": "cron-1",
      "name": "Cada 6 horas",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -260,
        640
      ]
    },
    {
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "REEMPLAZAR_ID_HOJA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Leads",
          "mode": "name"
        },
        "options": {}
      },
      "id": "gs-read2",
      "name": "CRM · Leer para seguimiento",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        -40,
        640
      ]
    },
    {
      "parameters": {
        "jsCode": "// Devuelve los leads que siguen 'nuevo' tras 48 h\nconst LIMITE_MS = 48 * 60 * 60 * 1000;\nconst ahora = Date.now();\nreturn $input.all().filter((i) => {\n  const f = i.json;\n  if (f.estado !== 'nuevo') return false;\n  const creado = Date.parse(f.fecha);\n  return !isNaN(creado) && (ahora - creado) >= LIMITE_MS;\n});\n"
      },
      "id": "code-2",
      "name": "Filtrar > 48 h sin contacto",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        180,
        640
      ]
    },
    {
      "parameters": {
        "chatId": "REEMPLAZAR_CHAT_ID",
        "text": "=⏰ Lead sin contactar hace +48 h: {{ $json.nombre }} ({{ $json.segmento }}) — {{ $json.email }}",
        "additionalFields": {}
      },
      "id": "tg-2",
      "name": "Equipo · Recordatorio de seguimiento",
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [
        400,
        640
      ]
    }
  ],
  "connections": {
    "Webhook · Nuevo lead": {
      "main": [
        [
          {
            "node": "CRM · Leer hoja",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM · Leer hoja": {
      "main": [
        [
          {
            "node": "Procesar lead",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Procesar lead": {
      "main": [
        [
          {
            "node": "¿Es lead nuevo?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "¿Es lead nuevo?": {
      "main": [
        [
          {
            "node": "CRM · Registrar lead",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Duplicado · Solo log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM · Registrar lead": {
      "main": [
        [
          {
            "node": "Newsletter · Suscribir",
            "type": "main",
            "index": 0
          },
          {
            "node": "Equipo · Avisar por Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cada 6 horas": {
      "main": [
        [
          {
            "node": "CRM · Leer para seguimiento",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "CRM · Leer para seguimiento": {
      "main": [
        [
          {
            "node": "Filtrar > 48 h sin contacto",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filtrar > 48 h sin contacto": {
      "main": [
        [
          {
            "node": "Equipo · Recordatorio de seguimiento",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {},
  "meta": {
    "instanceId": "club-stem-prod"
  },
  "tags": [
    {
      "name": "club-stem"
    },
    {
      "name": "leads"
    }
  ]
}